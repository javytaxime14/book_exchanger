<h3 class="text-center mb-5">Perfil de <%= @user.name %> <%= @user.last_name %></h3>
<div class="row">
    <div class="col-sm-6">
        <div class="panel panel-default">
            <div class="panel-heading">
            </div>
            <div class="panel-body"> 
                <p>Teléfono: <%= @user.phone_number %> </p>
                <p>Email: <%= @user.email %></p>
                <p>Miembro desde: <%= time_ago_in_words(@user.created_at) %> ago</p>
            </div>
        </div>
    </div>
    <div class="col-sm-6">
        <%= link_to 'Mis intercambios', exchanges_path, class: 'btn btn-primary' %>
    </div>
</div>